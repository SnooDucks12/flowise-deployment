# ✦ Upload here!

Drag your photos into this folder (GitHub: *Add file → Upload files*).

Rename the folder to match the real trip — keep the `YYYY-MM Name` format,
e.g. `2024-06 Amalfi Coast`. Make one folder per trip.

This README is ignored by the gallery — only images count.
